require 'spec_helper'

describe AdminController do

  describe "GET 'notify'" do
    pending "should be successful" do
      #get 'notify'
      #response.should redirect_to(login_path) #need to login first
      #login_user
      #follow_redirect!
    end
  end

end
