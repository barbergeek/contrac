<% if @opportunity.watched_by?(current_user) %>
$("#watch-opportunity-<%= @opportunity.id %>")
	.addClass("watched")
<% else %>
$("#watch-opportunity-<%= @opportunity.id %>")
	.removeClass("watched")
<% end %>