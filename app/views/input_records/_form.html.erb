<%= semantic_form_for(@input_record) do |f| %>
  <% if @input_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@input_record.errors.count, "error") %> prohibited this input_record from being saved:</h2>

      <ul>
      <% @input_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	<%= f.inputs %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
