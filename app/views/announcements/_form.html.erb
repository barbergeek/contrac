<%= simple_form_for(@announcement) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
		<%= f.input :title %>
    <%= f.input :content %>
    <%= f.input :author %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
