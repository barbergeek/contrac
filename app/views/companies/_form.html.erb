<%= simple_form_for(@company) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
		<%= f.input :abbreviation %>
    <%= f.input :certification_list %>
    <%= f.input :active %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
